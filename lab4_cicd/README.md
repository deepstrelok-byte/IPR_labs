# lab4
Библиотека с пользовательскими коллекциями
```markdown

## Технологический стек

*   **Язык:** Python 3.10
*   **Тестирование:** Pytest, Pytest-cov
*   **Контейнеризация:** Docker (Multi-stage build)
*   **CI/CD:** GitHub Actions (или GitLab CI/CD)

##  Структура проекта

```text
.
├── .github/
│   └── workflows/
│       └── ci-cd.yml      # Конфигурация пайплайна
├── src/
│   └── ...                # Исходный код приложения
├── tests/
│   └── ...                # Unit-тесты
├── Dockerfile             # Инструкция для сборки образа
├── main.py                # Точка входа в приложение
├── requirements.txt       # Зависимости для запуска
├── requirements-dev.txt   # Зависимости для тестирования
└── README.md              # Документация проекта
```

## 🚀 Как запустить локально

### Без Docker

1.  Создайте виртуальное окружение:
    ```bash
    python -m venv venv
    source venv/bin/activate  # Для Linux/Mac
    # venv\Scripts\activate   # Для Windows
    ```
2.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```
3.  Запустите приложение:
    ```bash
    python main.py
    ```

### С использованием Docker

1.  Соберите образ:
    ```bash
    docker build -t my-app .
    ```
2.  Запустите контейнер:
    ```bash
    docker run --rm my-app
    ```

## Тестирование

Для запуска тестов и проверки покрытия кода используйте:

```bash
pip install -r requirements-dev.txt
pytest --cov=src tests/
```

*Тесты покрывают более 70% функциональности приложения.*

## Описание CI/CD Пайплайна

Пайплайн настроен на автоматический запуск при пуше в ветку `main`. Он состоит из следующих этапов:

1.  **Unit Tests:**
    *   Установка зависимостей.
    *   Запуск `pytest` с генерацией отчета о покрытии.
    *   Если тесты падают, процесс останавливается.

2.  **Build & Push:**
    *   Запускается только после успешных тестов.
    *   Авторизация в DockerHub через Secrets.
    *   Сборка оптимизированного Docker-образа.
    *   Публикация образа с тегами `latest` и `SHA-коммита`.
```

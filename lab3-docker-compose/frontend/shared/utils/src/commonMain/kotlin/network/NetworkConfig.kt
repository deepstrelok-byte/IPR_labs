package network

@Suppress("ConstPropertyName")
object NetworkConfig {
//    const val host = "10.0.2.2" // `localhost` but from android studio emulator
    const val host = "localhost" // `localhost` but from android studio emulator

//                   val host = "10.230.221.145" // `localhost` but from device and wifi hotspot
    const val port = 8080
}